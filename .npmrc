# Hoist sharp from Next's optionalDependency so server code can import it
# without declaring a second direct dependency (avoids version conflicts).
public-hoist-pattern[]=sharp
public-hoist-pattern[]=@img/*
