model CourseVersion {
  id                String                   @id @default(cuid())
  courseId          String
  course            Course                   @relation(fields: [courseId], references: [id], onDelete: Cascade)
  version           Int
  publishedAt       DateTime                 @default(now())
  publishedById     String?
  publishedBy       User?                    @relation(fields: [publishedById], references: [id], onDelete: SetNull)
  /// Set only when the author asks at publish time; publishing alone leaves earlier versions takeable.
  superseded        Boolean                  @default(false)
  enrollments       CourseEnrollment[]
  lessons           Lesson[]
  scenes            Scene[]
  certificates      Certificate[]
  anonymousSessions AnonymousCourseSession[]

  @@unique([courseId, version])
  @@index([courseId])
  @@index([publishedById])
  @@map("course_version")
}
