export interface InvitationsPageTranslations {
  title: string;
  subtitle: string;
  searchPlaceholder: string;
  loading: string;
  emptyState: string;
  noMatches: string;
  clearFilters: string;
  card: {
    unknownOrg: string;
    invitedByAs: string;
    invitedAs: string;
    sent: string;
    expires: string;
    decline: string;
    accept: string;
  };
  toast: {
    accepted: string;
    failedAccept: string;
    rejected: string;
    failedReject: string;
  };
}
